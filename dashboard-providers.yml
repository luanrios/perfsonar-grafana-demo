apiVersion: 1

providers:
  - name: 'pscheduler dashboards'
    orgId: 1
    folder: 'Perfsonar Archive'
    type: file
    updateIntervalSeconds: 1000
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

